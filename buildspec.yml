version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo "Building..."
  post_build:
    commands:
      - echo "Deploying..."
      - chmod +x scripts/deploy.sh
      - ./scripts/deploy.sh